export default function HelloWorldPage() {
  return (
    <div>
      <h1>Hello from a Remote Component (Pages Router)</h1>
      <p>This component is served from expose-rc using Pages Router!</p>
    </div>
  );
}